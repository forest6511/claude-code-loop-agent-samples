# 仕様: 複数ファイル入力（id: multi-file）

- count が複数のファイル引数を受け取れるようにする
  例: python3 loglens.py count a.log b.jsonl
- 全ファイルの件数を合算して1つの集計として表示する（従来と同じ
  出力形式。ファイル別の内訳は表示しない）
- .log と .jsonl の混在を許可する（形式はファイルごとに判定する）
- 引数が1つのときの挙動・出力は従来と完全に同一に保つ
- --level / --since / --until との併用は、すべてのファイルに適用する
